{
  "id": "home-inventory",
  "version": "0.1.0",
  "schema": {
    "fields": [
      { "key": "item_name", "type": "string", "required": true },
      { "key": "location", "type": "string", "required": true },
      { "key": "condition", "type": "string", "required": false },
      { "key": "quantity", "type": "number", "required": false }
    ]
  },
  "extractionRules": [
    "以每件或每组可辨识物品为单位整理，保留原始照片关联。",
    "位置从照片说明、房间线索和用户指引中提取；不确定时标记待确认。",
    "数量仅在可数或用户明确描述时填写，避免估算。"
  ],
  "labels": {
    "template": "家庭物品盘点",
    "shotCount": "已记录照片",
    "summary": "盘点摘要",
    "item_name": "物品名称",
    "location": "存放位置",
    "condition": "物品状态",
    "quantity": "数量"
  }
}
